{
  "name": "asppibra-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter frontend dev",
    "dev:backend": "pnpm --filter backend dev",
    "build": "pnpm -r build",
    "install:all": "pnpm install"
  }
}
